name: Run Volleystats Workflow

on:
  workflow_dispatch:
    inputs:
      workflow:
        description: 'Select the workflow to run'
        required: true
        default: vbl-full
        type: choice
        options:
          - vbl-full
      log:
        description: 'Enable verbose Scrapy logging'
        required: false
        default: false
        type: boolean

permissions:
  contents: write  # wichtig, um Push ins Repo zu erlauben

jobs:
  run-workflow:
    name: Execute selected workflow
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run workflow
        env:
          WORKFLOW_CHOICE: ${{ github.event.inputs.workflow }}
          LOG_ENABLED: ${{ github.event.inputs.log }}
        run: |
          set -euo pipefail
          LOG_FLAG=""
          if [ "${LOG_ENABLED}" = "true" ]; then
            LOG_FLAG="--log"
          fi
          python -m volleystats.main --fed vbl --workflow "${WORKFLOW_CHOICE}" ${LOG_FLAG}

      - name: Upload collected data as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: volleystats-data
          path: data
          if-no-files-found: warn

      - name: Commit and push CSVs
        if: success() && (hashFiles('data/**/*.csv') != '')
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          mkdir -p docs/data
          cp -r data/*.csv docs/data/ || echo "No CSV files found"
          git add docs/data/*.csv || echo "Nothing to add"
          git commit -m "chore: update volleystats CSV data [skip ci]" || echo "No changes to commit"
          git push
